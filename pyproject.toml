[project]
name = "audio-identification-hackathon"
version = "0.1.0"
description = "Dependency-light audio identification and source detection baseline"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
